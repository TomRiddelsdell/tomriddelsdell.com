# CRITICAL: OpenNext Cloudflare requires REAL flat node_modules (no symlinks)
# node-linker=hoisted creates an npm-like flat structure
# This is required because OpenNext's esbuild bundler can't follow pnpm symlinks
node-linker=hoisted
shamefully-hoist=true
